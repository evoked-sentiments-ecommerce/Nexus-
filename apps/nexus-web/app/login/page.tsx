import LoginForm from '@/components/LoginForm';

export const metadata = {
  title: 'Login - Nexus',
  description: 'Login to your Nexus account',
};

export default function LoginPage() {
  return <LoginForm />;
}
